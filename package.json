{
  "name": "prettier-pre-commit",
  "private": true,
  "version": "1.0.5",
  "description": "Prettier hook for pre-commit (formats JSON, YAML, Markdown, TOML).",
  "license": "MIT",
  "devDependencies": {
    "prettier": "3.6.2",
    "prettier-plugin-toml": "^0.3.5"
  },
  "scripts": {
    "fmt": "prettier --write --ignore-unknown"
  }
}
