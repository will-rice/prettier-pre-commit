{
  "name": "prettier-pre-commit",
  "private": true,
  "version": "1.0.8",
  "description": "Prettier hook for pre-commit (supports all Prettier file formats).",
  "license": "MIT",
  "devDependencies": {
    "prettier": "3.6.2",
    "prettier-plugin-toml": "^2.0.6"
  },
  "scripts": {
    "fmt": "prettier --write --ignore-unknown"
  }
}
